<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nono.concesionariocoches">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ConcesionarioCoches"
        android:usesCleartextTraffic="true">
        <activity android:name="controller.imgProfile"></activity>
        <activity android:name="controller.ProfileActivity" />
        <activity android:name="controller.RegistrarActivity" />
        <activity android:name="controller.CatalogoVehiculosActivity" />
        <activity android:name="controller.LoginActivity" />
        <activity android:name="controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>